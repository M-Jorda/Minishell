#include "../lexer.h"

char	*expand_variable(char *var_name)
{
	// Expand $VAR variables
	return (NULL);
}

char	*process_var_expansion(char *input)
{
	// Process all variable expansions in input
	return (NULL);
}
