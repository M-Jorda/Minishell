#include "../lexer.h"

char	**expand_wildcard(char *pattern)
{
	// Expand * wildcards to matching files
	return (NULL);
}

void	process_wildcard_expansion(t_token_list *tokens)
{
	// Process wildcard expansions in token list
}
