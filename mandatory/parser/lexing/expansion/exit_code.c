#include "../lexer.h"

char	*expand_exit_code(void)
{
	// Expand $? to last exit code
	return (NULL);
}

char	*process_exit_code_expansion(char *input)
{
	// Process $? expansions in input
	return (NULL);
}
