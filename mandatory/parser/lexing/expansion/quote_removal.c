#include "../lexer.h"

char	*remove_quotes(char *input)
{
	// Remove quotes after expansion
	return (NULL);
}

void	process_quote_removal(t_token_list *tokens)
{
	// Remove quotes from all tokens after expansion
}
