#include "parser.h"

int	get_operator_precedence(t_token_type type)
{
	// Get operator precedence level
	return (0);
}

int	is_left_associative(t_token_type type)
{
	// Check if operator is left associative
	return (1);
}
