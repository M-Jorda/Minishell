#include "parser.h"

t_ast_node	*parse_parentheses(t_token **current)
{
	// Parse grouped expressions in parentheses
	return (NULL);
}

int	check_balanced_parentheses(t_token_list *tokens)
{
	// Check if parentheses are balanced
	return (1);
}
