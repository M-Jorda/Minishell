#include "parser.h"

t_ast_node	*parse_redirection(t_token **current)
{
	// Parse redirection operators
	return (NULL);
}

int	is_redirection_token(t_token_type type)
{
	// Check if token is a redirection operator
	return (0);
}
