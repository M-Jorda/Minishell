#include "parser.h"

t_ast_node	*parse_pipeline(t_token **current)
{
	// Parse pipe operators
	return (NULL);
}

t_ast_node	*parse_logical_operators(t_token **current)
{
	// Parse && and || operators
	return (NULL);
}
