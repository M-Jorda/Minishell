#include "parser.h"

t_ast_node	*parse_simple_command(t_token **current)
{
	// Parse simple command
	return (NULL);
}

t_ast_node	*parse_command_args(t_token **current)
{
	// Parse command arguments
	return (NULL);
}
